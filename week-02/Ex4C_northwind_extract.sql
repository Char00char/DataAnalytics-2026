-- Prodcuts holds the items that Northwind sellls
-- Categories hold the types/categories of items
SELECT * FROM employees; 
-- Margaret Peacock is the Northwind employee whose name makes it look like she’s a bird
SELECT * FROM products;
-- There at 77 records, 
SELECT * FROM categories;
-- The CategoryID for Seafood is 8
SELECT OrderID, OrderDate, ShipName, ShipCountry FROM orders;

